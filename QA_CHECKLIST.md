# QA 체크리스트

**하나라도 실패하면 Editor로 반송한다. 최대 3회 반복. 통과 전 저장 금지.**

각 항목은 판정 근거를 함께 적는다. "괜찮아 보임" 같은 판정은 무효다.

---

## A. 분량 (하드 게이트 — 실측 필수)

- [ ] `python3 scripts/count.py <파일>` 을 **실제로 실행**했다. 눈대중 추정이 아니다.
- [ ] 공백 포함 본문 글자 수가 **2000자 이하**다. (초과 시 저장 금지)
- [ ] 글자 수가 목표 구간 **1400~1900자** 안에 있다.
- [ ] 프론트매터 `chars` 값이 실측치와 일치한다.

## B. 종결어미

- [ ] 모든 문장이 `~이다 / ~였다 / ~한다 / ~않다` 계열 평서형으로 끝난다.
- [ ] 경어체(`습니다`, `합니다`, `해요`, `이에요`)가 한 번도 없다.
- [ ] 청유형·명령형이 없다.

## C. 메타 언급

- [ ] 금지 표현이 하나도 없다: `이 글에서는`, `이야기해보려`, `공유하고자`, `여러분`, `독자`, `일기처럼`, `개인적인 생각을 적어보면`, `정리해보자면`
- [ ] 마지막 문단이 교훈 요약이 아니다.
- [ ] 마지막 문단이 행동 촉구가 아니다.
- [ ] 마지막 문단이 질문 던지기로 끝나지 않는다.

## D. 상투어

- [ ] `결국` 남발이 없다 (2회 이하).
- [ ] `진정한`이 없다.
- [ ] `~이 아닐까 싶다` 남발이 없다 (1회 이하).
- [ ] 닳은 비유(`인생은 마라톤` 류)가 없다.

## E. 내용

- [ ] Thinker가 고른 각도가 실제로 글에 살아 있다. 표면적 결론으로 닫히지 않았다.
- [ ] 겪은 일 또는 판단이 바뀐 지점이 최소 하나 들어 있다.
- [ ] 불확실한 숫자·인용·사실 주장을 단정하지 않았다.
- [ ] 소제목이 없다.

## F. 형식

- [ ] 파일 경로가 `posts/YYYY-MM-DD-슬러그.md` 형식이다.
- [ ] 날짜가 `date` 명령 실측 결과와 일치한다.
- [ ] 프론트매터에 `title`, `date`, `slug`, `topic`, `chars`가 모두 있다.
- [ ] `title`이 요약형이 아니라 글에서 가장 뾰족한 한 문장에서 나왔다.

---

## 판정

- 전 항목 통과 → `PASS`. Orchestrator가 저장 후 알림.
- 하나라도 실패 → `FAIL`. 실패 항목과 해당 문장을 명시해 Editor로 반송.
- 3회 반송 후에도 실패 → 실패 항목을 글 하단 메모에 남기고 저장한다.
  단 **분량 2000자 초과만은 예외 없이 저장 금지**이며, 통과할 때까지 Editor를 계속 돌린다.
