# 문체 규칙

Writer와 Editor에 공통 적용된다. **위반은 실패 처리**이며 QA에서 Editor로 반송된다.

---

## 1. 종결어미

모든 문장은 아래 형태의 **평서형 종결**로 쓴다.

- `~이다` / `~였다` / `~한다` / `~않다`

금지:

- 경어체 — `~습니다`, `~합니다`, `~해요`, `~이에요`
- 청유형 — `~하자`, `~해보자`
- 명령형 — `~해라`, `~하라`

## 2. 시점과 태도

- **혼자 적는 기록처럼** 쓴다.
- 독자를 부르지 않는다. 가르치려 들지 않는다.
- 겪은 일, 판단이 바뀐 지점, 아직 정리되지 않은 의문을 그대로 적는다.
- 결론을 억지로 봉합하지 않아도 된다. 열린 채 끝나도 된다.

## 3. 메타 언급 전면 금지

글의 성격을 글 안에서 언급하지 않는다. 아래 표현은 전부 금지다.

- `이 글에서는`
- `~에 대해 이야기해보려 한다`
- `공유하고자 한다`
- `여러분`
- `독자`
- `일기처럼`
- `개인적인 생각을 적어보면`
- `정리해보자면`

마무리에서 아래를 하지 않는다.

- 교훈 요약
- 행동 촉구
- 질문 던지며 끝내기

## 4. 상투어 금지

- `결국` 남발
- `진정한`
- `~이 아닐까 싶다` 남발
- `인생은 마라톤` 류의 닳은 비유

## 5. 사실 주장

- 숫자, 인용, 사실 주장은 **확실한 것만** 쓴다.
- 불확실하면 단정하지 말고 아예 빼거나 `정확히 기억나지 않는다`로 처리한다.
- 책 제목이나 인물명을 확신할 수 없으면 쓰지 않는다.

## 6. 형식

- 소제목을 원칙적으로 쓰지 않는다.
- 불릿 목록으로 글을 구성하지 않는다.
- 문단은 하나의 생각이 번지는 단위로 나눈다.
